export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export enum DirectionScroll {
  front = 'front',
  behind = 'behind',
}

export enum SizeType {
  init = 'init',
  static = 'static',
  dynamic = 'dynamic',
}

